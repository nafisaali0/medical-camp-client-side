
const HomeTitle = ({title}) => {
    return (
        <>
            <div>
                <h1 className="text-2xl font-bold text-right">{title}</h1>
            </div>
        </>
    );
};

export default HomeTitle;